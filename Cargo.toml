[package]
name = "risk-harbor"
version = "0.1.0"
edition = "2024"
description = "Insurance operations domain core with integer-safe calculations and PostgreSQL persistence"

[dependencies]
tokio = { version = "1.48", features = ["macros", "rt-multi-thread"] }
tokio-postgres = "0.7"

[profile.release]
lto = true
codegen-units = 1
strip = true
